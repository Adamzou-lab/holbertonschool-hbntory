SECRET_KEY=dev-secret-change-me
ADMIN_EMAIL=admin@stockflow.local
ADMIN_PASSWORD=changeme123
BACKOFFICE_INTERNAL_TOKEN=change-me-shared-secret

# URL de l'API Produit externe (hbntory-products-api), lancée séparément
# via son propre docker-compose sur la machine hôte. Laisser tel quel si
# elle tourne sur localhost:5001 côté hôte (Docker Desktop uniquement).
PRODUCTS_API_BASE_URL=http://host.docker.internal:5001
